extends ../layout 

block content
    ul(class="profile-pic-list")
        each employee in employees
            li
                img(src="#{employee.Photograph__c}")
                div
                    h2= employee.Name
                    h3= employee.Title
                    a(href="tel:#{employee.Phone}")
                        p= employee.Phone
        